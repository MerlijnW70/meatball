node_modules/
dist/
target/
*.log
.env
.env.local
.env.*.local
# Module bindings worden per `spacetime publish` + `npm run generate` ververst,
# maar moeten in git zitten zodat Vercel ze kan bouwen zonder spacetime CLI.
# Voor lokale dev: na server-publish `npm run generate` draaien en committen.

# Scraped HTML — regenereerbaar via curl loop in seed_data/generate.py
server/seed_data/hv_pages/

# OS
.DS_Store
Thumbs.db

# Editor / tooling
.vscode/
.idea/
.claude/
