# Production config — gebruikt door `npm run build` (Vite production mode).
# Lokaal met `npm run dev` wordt .env.local (of defaults) gebruikt.

VITE_STDB_HOST=wss://maincloud.spacetimedb.com
VITE_STDB_MODULE=meatball
